import { makeStyles } from '@material-ui/core/styles'

export const useStyles = makeStyles({
  icon: {
    fontSize: 20,
  },
  iconVariant: {
    opacity: 0.9,
    marginRight: 8,
  },
  message: {
    display: 'flex',
    alignItems: 'center',
    maxWidth: '400px',
  },
})
